# Content Recovery 2026-08-06

## Snapshot

Before recovery, a full database snapshot was written to:

`/var/backups/adammuiz-recovery-20260806/pre-recovery.sql.gz`

SHA-256:

`326f93ed418637ec633a7d0a0ce5d2fbe9b709f1830b713ef42ea97eb82f851a`

After recovery, a second snapshot was written to:

`/var/backups/adammuiz-recovery-20260806/post-recovery/restored.sql.gz`

SHA-256:

`679dcf467b45bfb34b27c24460419aef4c51e844c00b72d6b0ed7b05a10c91b1`

## Recovered

The English source records below were rebuilt from their preserved published Indonesian translations. Their Indonesian and German translations were retained.

- Pages: `118` (About), `119` (Contact), `120` (Payment)
- Articles: `175` through `207`, excluding `208` through `212` which are absent, plus `213` and `215`

An additional 24 articles were recovered from their original HTML payloads in the local OpenCode session database: `146-148`, `150-155`, and `160-174`. Their original Indonesian HTML was restored exactly before English and German translations were created.

Every source record was updated by its explicit `posts.id` inside an individual transaction. The switcher was verified on representative pages and articles after recovery.

## Still Missing A Source

These records were overwritten and do not have a published Indonesian translation from which to recover the original content:

`125-131`, `132`, `133-137`, `139-145`, `149`

This includes 17 articles, one page, one theme post, and two shortcode presets. Their original title, slug, and content require an external source, such as a server/provider snapshot, exported database, a public archive, or a historical CMS export.

## Safety Rules

1. Do not run concurrent writers against the content database.
2. Create a new SQL snapshot before every recovery batch.
3. Use explicit `WHERE id = ?` updates only.
4. Verify the source, Indonesian, and German route before marking a record restored.
